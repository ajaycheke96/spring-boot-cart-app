spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    show-sql: true
  liquibase:
      enabled: false

multitenancy:
  master:
    datasource:
      driverClassName: com.mysql.cj.jdbc.Driver
      jdbcUrl: jdbc:mysql://localhost:3306/diamond?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
      username: diamond
      password: da123